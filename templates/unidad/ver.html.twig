{% extends 'base.html.twig' %}

{% block title %}Unidad{% endblock %}

{% block body %}
    <h1>Unidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unidad.id }}</td>
            </tr>
            <tr>
                <th>Edificio</th>
                <td>{{ unidad.edificio }}</td>
            </tr>
            <tr>
                <th>Piso</th>
                <td>{{ unidad.piso }}</td>
            </tr>
            <tr>
                <th>Unidad</th>
                <td>{{ unidad.unidad }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ unidad.estado ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unidades_index') }}">back to list</a>

    <a href="{{ path('unidades_editar', {'id': unidad.id}) }}">edit</a>

    {{ include('unidad/_delete_form.html.twig') }}
{% endblock %}
